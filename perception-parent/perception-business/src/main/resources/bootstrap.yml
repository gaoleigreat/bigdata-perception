server:
  port: 18070
  servlet:
    context-path: /
spring:
  application:
    name: business-service
  profiles:
    active: local
  cloud:
    stream:
      bindings:
        output_log:
          destination: printLog
          content-type: application/json


application:
  description: 业务服务


logging:
  level:
    com:
      lego:
        framework:
          template:
            feign:
              TemplateFeignClient: debug