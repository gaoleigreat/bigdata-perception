server:
  port: 18070
  servlet:
    context-path: /
spring:
  application:
    name: business-service
  profiles:
    active: local


application:
  description: 业务服务


logging:
  level:
    com:
      lego:
        framework:
          template:
            feign:
              TemplateFeignClient: debug