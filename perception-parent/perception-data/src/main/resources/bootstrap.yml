server:
  port: 18000
  servlet:
    context-path: /
spring:
  application:
    name: data-service
  profiles:
    active: local
  cloud:
    stream:
      bindings:
        output_log:
          destination: printLog
          content-type: application/json


application:
  description: 数据服务