spring:
  cloud:
    config:
      fail-fast: true
      uri: http://192.168.0.86:48020
      profile: sit
      label: master
      username: admin
      password: admin123
      retry:
        max-attempts: 3


  security:
    user:
      name: ${define.user.name}
      password: ${define.user.password}