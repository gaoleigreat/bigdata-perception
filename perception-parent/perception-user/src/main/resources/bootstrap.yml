server:
  port: 18050
  servlet:
    context-path: /
spring:
  application:
    name: user-service
  profiles:
    active: local
  cloud:
    stream:
      bindings:
        output_log:
          destination: printLog
          content-type: application/json


application:
  description: 用户服务