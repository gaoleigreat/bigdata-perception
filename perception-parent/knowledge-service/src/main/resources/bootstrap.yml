server:
  port: 38020
  servlet:
    context-path: /
spring:
  application:
    name: knowledge-service
  profiles:
    active: local
  cloud:
    stream:
      bindings:
        output_log:
          destination: printLog
          content-type: application/json


application:
  description: 知识服务