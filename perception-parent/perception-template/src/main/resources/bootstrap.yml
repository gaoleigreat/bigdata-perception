server:
  port: 18080
  servlet:
    context-path: /
spring:
  application:
    name: template-service
  profiles:
    active: local
  cloud:
    stream:
      bindings:
        output_log:
          destination: printLog
          content-type: application/json


application:
  description: 模板服务