server:
  port: 38010
  servlet:
    context-path: /
spring:
  application:
    name: equipment-service
  profiles:
    active: local


application:
  description: 设备服务


logging:
  level:
    com:
      lego:
        framework:
          business:
            feign:
              CurdClient: debug
