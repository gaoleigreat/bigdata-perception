server:
  port: 38010
  servlet:
    context-path: /
spring:
  application:
    name: equipment-service
  profiles:
    active: local
  cloud:
    stream:
      bindings:
        output_log:
          destination: printLog
          content-type: application/json


application:
  description: 设备服务